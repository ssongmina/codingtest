#include <bits/stdc++.h>

using namespace std;

long double a,b;

int main(){
	ios_base::sync_with_stdio(false);
	cin.tie(NULL); cout.tie(NULL);
	
	cin >> a >> b;
	
	
	cout.precision(15);
    cout << fixed; 
    cout << a / b << endl;
	
	return 0;
}